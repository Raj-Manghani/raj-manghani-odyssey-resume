# Git files
.git
.gitignore

# Node modules (will be installed inside container)
node_modules
terminal-backend/node_modules

# Build artifacts (if any, besides dist which is handled in frontend Dockerfile)
# build

# OS files
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (should be passed via env vars)
.env*
